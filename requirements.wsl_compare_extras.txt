# Extra packages for running SEQ's compare matrix from the WSL LightCompress venv.
# Install after LightCompress requirements.txt and requirements.lm_eval.txt.
# Keep torch, transformers, datasets, accelerate, and huggingface-hub owned by
# the LightCompress environment to avoid fighting its resolver.
bitsandbytes==0.49.1
hf-xet==1.2.0
matplotlib==3.10.8
pandas==3.0.2
psutil==7.2.2
PyYAML==6.0.3
safetensors==0.7.0
sentencepiece==0.2.1
tokenizers==0.22.2
tiktoken==0.12.0
tqdm==4.67.1
