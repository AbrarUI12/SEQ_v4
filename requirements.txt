# SEQ — pinned environment for the audit pipeline.
# Versions are those of the environment that produced the committed results
# (Python 3.12.3, CUDA 12.4). Install torch first from the cu124 index:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#
# CPU-only analysis (rebuilding tables/figures from committed JSON) needs only
# matplotlib and numpy; everything else is required to run sweeps/evaluations.

torch==2.6.0
transformers==5.14.1
tokenizers==0.22.2
huggingface-hub==1.24.0
datasets==5.0.0
accelerate==1.14.0
safetensors==0.8.0
hqq==0.2.8.post1
lm_eval==0.4.12
matplotlib==3.11.1
numpy==2.5.1
pytest
